# Environments

Flat, gap, and step geometry is generated by `ModelBuilder` from
`config/environment.toml`. Task/crossing metrics live separately in
`bari_sim.environment.EnvironmentEvaluator`, so policy observations never receive
privileged world coordinates or map dimensions.

